# Otithee — Full Prototype Gap Analysis, Logic Fix & Backend-Ready Implementation

You are working on the existing **Otithee** project.

Your task is to **audit the entire existing application, identify missing modules/features and broken/inconsistent logic, then implement the required improvements directly into the existing prototype.**

## Critical Constraint

**Do NOT add a real database or backend in this phase.**

Everything must continue working as a **frontend prototype with local/mock/in-memory data**.

However, the implementation must be structured so that later we can connect real backend APIs with minimal changes.

The prototype must therefore behave like a real production system from the user's perspective while keeping persistence mocked.

---

# 1. FIRST: Understand the Existing Project

Before modifying anything:

* Inspect the complete project structure.
* Identify the routing architecture.
* Identify all modules/features.
* Identify shared components.
* Identify state management.
* Identify mock/seed/domain data.
* Identify validation schemas.
* Identify RBAC/permissions.
* Identify booking/business logic.
* Identify merchant/vendor logic.
* Identify financial/commission logic.
* Identify catalogue/listing logic.
* Identify admin dashboard logic.
* Identify customer-facing flows.
* Identify existing reusable UI patterns.

Do NOT blindly rewrite existing code.

Preserve working functionality.

---

# 2. Use the Existing Analysis as a Required Reference

The repository contains an existing vendor/merchant ecosystem analysis.

Use it as one of the primary references when auditing the implementation.

Important findings include:

* Merchant onboarding is missing.
* KYC/verification is missing.
* Merchant bank/payout information is missing.
* Contract/commercial-term acceptance is missing.
* `MerchantRef` and `Merchant` are currently two contradictory merchant models.
* Commission units are inconsistent:

  * domain merchant commission uses percentage values such as `12`
  * merchant module uses ratio values such as `0.08`
* Catalogue approval workflow is missing.
* Merchant staff/sub-user management is missing.
* Channel manager integration is missing.
* Merchant onboarding progress/health is missing.
* Payout schedule/statements are incomplete.
* Merchant-facing advertising purchase flow is missing.
* Merchant subscription plans are missing.
* Merchant performance dashboards are incomplete.
* Multi-property grouping is missing.
* Merchant dispute participation is missing.
* Merchant API/PMS integration is future-facing and should be represented architecturally, but do NOT build a real backend integration now.

Treat the analysis as a starting point, NOT as the only source of truth.

Verify every finding against the actual repository before changing anything.

---

# 3. Create an Internal Gap Matrix Before Coding

First inspect the project and classify each discovered feature as:

* IMPLEMENTED
* PARTIALLY IMPLEMENTED
* MISSING
* BROKEN
* CONTRADICTORY
* MOCK ONLY
* FUTURE/BACKEND DEPENDENT

Focus on:

### Merchant ecosystem

* Merchant registration
* Merchant application
* Verification/KYC
* Business profile
* Documents
* Contract acceptance
* Bank/payout details
* Onboarding checklist
* Merchant status
* Catalogue/product creation
* Catalogue approval
* Publishing
* Inventory
* Pricing
* Availability
* Booking management
* Fulfilment
* Customer management
* Staff management
* Permissions
* Commission
* Settlement
* Payout
* Reports
* Analytics
* Promotions
* Coupons
* Advertising
* Reviews
* Notifications
* Support
* Subscription plans
* Multi-property management
* Disputes

### Platform/admin

* Merchant approval
* Merchant verification
* Catalogue approval
* Commission management
* Settlement management
* Payout management
* Advertising management
* Subscription management
* Revenue management
* Platform revenue
* B2B revenue
* Insurance revenue
* Other monetisation modules
* System configuration

### Customer/booking ecosystem

Audit all existing booking types and flows.

Do not assume a feature exists simply because a route or menu item exists.

Trace the complete flow:

UI → state → validation → mock/domain data → business logic → resulting UI state.

---

# 4. Fix Contradictory Domain Models

This is a HIGH PRIORITY task.

If the repository still contains separate/conflicting merchant representations such as:

* `MerchantRef`
* `Merchant`

identify which model is authoritative.

Create a single canonical frontend domain representation.

All of these must use the same merchant source:

* bookings
* commission
* settlements
* payouts
* merchant dashboard
* admin merchant management
* merchant catalogue
* merchant analytics
* merchant promotions
* merchant advertising
* merchant profile

Do not maintain two independent merchant datasets.

If adapters/mappers are necessary, create them explicitly.

Do NOT perform a huge unnecessary rewrite.

---

# 5. Normalize Commission Logic

Audit every place where commission is calculated.

Choose ONE canonical representation.

Recommended:

```ts
commissionRate: number // percentage, e.g. 12 means 12%
```

Use helper functions for conversion/calculation instead of scattering arithmetic throughout the UI.

For example:

```ts
calculateCommission(...)
calculateMerchantNet(...)
calculatePlatformRevenue(...)
```

Make sure:

* booking commission
* merchant negotiated commission
* product default commission
* settlement
* refund adjustment
* reporting
* payout

all use the same calculation rules.

Do not allow `12` and `0.12` to silently mean different things in different modules.

---

# 6. Implement Missing Merchant Onboarding

Build a complete frontend prototype flow:

Registration
→ Business information
→ Contact information
→ Business documents
→ Verification/KYC
→ Commercial terms
→ Bank/payout details
→ Review
→ Submission
→ Admin approval/rejection
→ Onboarding completion
→ Merchant dashboard

The UI should behave like a real workflow.

Include realistic states:

* Draft
* Submitted
* Under Review
* Action Required
* Approved
* Rejected
* Suspended

Use mock data/state only.

Do NOT pretend that real KYC verification or banking verification is happening.

---

# 7. Implement KYC / Document Management

Create a prototype-ready structure for:

* business registration
* tax ID
* ownership information
* licences
* identity documents
* supporting documents

Include:

* upload UI
* document status
* verification state
* rejection reason
* re-upload flow

Since there is no backend:

Use mock upload behavior and local state.

But keep the domain structure API-friendly.

Example conceptual structure:

```ts
MerchantDocument {
  id
  merchantId
  type
  fileName
  fileUrl
  status
  rejectionReason
  uploadedAt
  verifiedAt
}
```

Do not implement fake real-world verification.

---

# 8. Implement Merchant Onboarding Progress

Create an onboarding progress/checklist system.

Track items such as:

* Business profile
* Contact details
* Documents
* KYC
* Contract
* Bank details
* First property/product
* Catalogue approval
* Publishing

Show:

* completion percentage
* pending actions
* blocked actions
* next recommended action

The same underlying state should drive both UI and business logic.

---

# 9. Implement Catalogue Approval Workflow

Currently product creation and public publishing are disconnected.

Fix this.

The lifecycle should support:

```text
Draft
→ Submitted
→ Under Review
→ Approved
→ Published
```

and:

```text
Submitted
→ Rejected
→ Action Required
→ Resubmitted
```

Include rejection reasons.

Admin should be able to:

* review
* approve
* reject
* request changes

Merchant should be able to:

* create draft
* submit
* see review status
* fix rejected content
* resubmit

Public catalogue should only expose approved/published items.

Do not duplicate catalogue data into unrelated static arrays.

---

# 10. Merchant Staff & Roles

Add merchant-side staff management.

Support realistic roles such as:

* Owner
* Manager
* Reservations
* Front Desk
* Revenue Manager
* Finance

Implement frontend RBAC.

Permissions must be checked centrally, not scattered randomly through components.

A staff member should not automatically have owner-level access.

Keep the architecture ready for real authentication later.

---

# 11. Merchant Finance

Audit and improve:

* commissions
* gross booking value
* merchant earnings
* platform revenue
* settlements
* refunds
* adjustments
* payouts
* payout status
* payout schedule
* statements

Create realistic prototype states.

For example:

```text
Pending
Processing
Paid
Failed
On Hold
```

Statements should be generated from the same underlying financial data used by the dashboard.

Do not create separate hard-coded numbers for each screen.

---

# 12. Platform Revenue Management

The prototype should clearly model how Otithee makes money.

Where applicable, support:

### Booking commission

Merchant booking → commission → platform revenue → merchant net

### Advertising

Merchant purchases campaign → campaign spend → platform advertising revenue

### Premium membership/subscription

Merchant/customer subscription → subscription revenue

### B2B

Agency/business booking → B2B commission/service revenue

### Insurance

Insurance-related booking/add-on → platform commission/revenue

### Other service revenue

Transport, tours, activities, etc. should be represented consistently.

Important:

Do not invent arbitrary financial calculations.

Use configurable mock rules and clearly defined formulas.

---

# 13. Merchant Advertising

Existing analysis indicates advertising logic exists on the admin side but merchant self-service is missing.

Implement merchant-facing advertising UI using the existing advertising engine where possible.

Merchant should be able to:

* view available advertising products
* create campaign
* select listing/product
* choose budget
* choose pricing model
* set campaign dates
* preview estimated spend
* submit campaign
* see campaign status
* see spend/performance

Admin remains responsible for platform-level controls.

Use mock billing only.

---

# 14. Merchant Subscription Plans

Add a prototype merchant subscription system.

Example plan structure:

* Basic
* Professional
* Premium

Do NOT hard-code plan logic inside UI components.

Create a domain configuration such as:

```ts
SubscriptionPlan {
  id
  name
  price
  billingCycle
  features
  limits
}
```

Subscription should influence merchant capabilities where appropriate.

Payment must remain mocked.

---

# 15. Multi-Property Merchant Support

A merchant should be able to own/manage multiple properties or supply entities.

Create the structure:

```text
Merchant
 ├── Property A
 ├── Property B
 └── Property C
```

Merchant-level data and property-level data must remain conceptually separate.

This is important for future PMS/API integration.

---

# 16. Merchant Performance / Health

Add a meaningful merchant performance section where appropriate.

Potential metrics:

* bookings
* revenue
* conversion
* cancellation rate
* response rate
* review score
* listing completeness
* ranking/performance indicators

Do not invent fake precision unnecessarily.

Use clearly marked mock/demo data.

---

# 17. Disputes

Audit the existing dispute functionality.

Merchants should be able to:

* see relevant disputes
* view dispute details
* respond
* submit supporting information
* track status

Platform/admin retains final authority.

---

# 18. Channel Manager / PMS

Do NOT build a real integration.

But make sure the architecture can support:

```text
Merchant
→ Property
→ External PMS / Channel Manager
→ Inventory
→ Rates
→ Availability
→ Reservations
```

Add an appropriate prototype UI/status if the existing product structure supports it.

Clearly distinguish:

* Connected
* Not Connected
* Syncing
* Error

No fake external API calls.

---

# 19. Notifications

Audit the notification system.

Create a centralized notification model that can later be replaced by API/WebSocket/push events.

Support relevant events such as:

* merchant application submitted
* verification required
* application approved
* application rejected
* catalogue approved
* catalogue rejected
* booking received
* booking cancelled
* payout processed
* dispute opened
* subscription expiring

For now use mock/local event generation.

---

# 20. Prototype Data Architecture

This is extremely important.

Do NOT scatter mock objects throughout components.

Create a clean separation such as:

```text
domain/
mock/
services/
repositories/
state/
ui/
```

Exact folder structure may differ according to the existing project.

The goal is:

```text
UI
 ↓
Service / Use Case
 ↓
Repository / Data Source
 ↓
Mock Data
```

Later:

```text
UI
 ↓
Service / Use Case
 ↓
Repository / API
 ↓
Backend
```

The UI should NOT need major rewrites when backend APIs are introduced.

---

# 21. Repository/API-Ready Pattern

Where practical, create interfaces such as:

```ts
MerchantRepository
BookingRepository
CatalogueRepository
SettlementRepository
PayoutRepository
AdvertisingRepository
SubscriptionRepository
```

For now provide mock implementations.

Do NOT over-engineer this.

Only introduce abstractions where they provide a real boundary between UI/business logic and data access.

---

# 22. Validation

Use the existing validation approach consistently.

All forms must validate:

* required fields
* invalid values
* invalid business states
* duplicate/invalid actions
* submission requirements

Do not rely only on button disabling.

---

# 23. State Consistency

Audit for:

* duplicate sources of truth
* stale state
* state resetting unexpectedly
* page reload inconsistencies
* route navigation losing state
* modal state leaks
* incorrect optimistic updates
* mock data becoming inconsistent between screens

A change made in one module should be reflected wherever that domain entity is used.

Example:

If a merchant is approved:

* admin merchant list changes
* merchant status changes
* onboarding completion updates
* merchant dashboard becomes available
* approved catalogue can progress
* relevant notifications appear

Do not update only the current page.

---

# 24. Existing Features Must NOT Break

Before implementing anything:

identify existing working functionality.

After implementation verify:

* authentication UI
* customer flows
* booking flows
* availability
* inventory
* pricing
* commission
* settlement
* reports
* analytics
* promotions
* coupons
* reviews
* support
* admin features
* merchant features

Do not remove existing capabilities simply because they are not part of the new work.

Reuse existing components wherever reasonable.

---

# 25. UX Requirements

The prototype should feel like a real SaaS marketplace.

Follow the existing Otithee visual language.

Do not introduce random new design systems.

Use:

* consistent spacing
* existing buttons
* existing cards
* existing tables
* existing modals
* existing form components
* existing typography
* existing colors

Handle:

* loading
* empty
* success
* error
* disabled
* pending
* rejected
* approval-required
* permission-denied

states properly.

---

# 26. No Fake Production Claims

Because this is a frontend prototype:

Do NOT implement fake:

* real payments
* real bank transfers
* real KYC verification
* real document verification
* real email delivery
* real SMS
* real external PMS integration
* real insurance API
* real ad billing

Instead create realistic mock flows with clear internal abstractions.

---

# 27. Backend-Ready Rule

Every new feature must answer:

1. What is the domain entity?
2. What data does it need?
3. What state transitions exist?
4. What actions can the user perform?
5. What permissions are required?
6. What data would eventually come from an API?
7. What data would eventually be submitted to an API?

Keep these boundaries clean.

---

# 28. Testing / Verification

After implementation:

Run the existing:

* typecheck
* lint
* build
* tests

Fix all errors introduced by your changes.

Then manually verify the major flows.

At minimum verify:

### Merchant

Registration
→ onboarding
→ KYC
→ contract
→ payout details
→ submission
→ admin review
→ approval/rejection
→ dashboard

### Catalogue

Create
→ draft
→ submit
→ admin review
→ approve/reject
→ resubmit
→ publish

### Booking

Customer booking
→ merchant booking
→ commission
→ settlement
→ payout

### Advertising

Merchant
→ campaign
→ budget
→ status
→ spend/performance

### Subscription

Merchant
→ plan
→ subscription
→ feature/limit behavior

### Permissions

Owner vs Manager vs Staff vs Admin

---

# 29. Important Implementation Strategy

Work in phases.

## Phase 1 — Audit

Inspect the repository and identify:

* missing
* broken
* contradictory
* duplicated
* hard-coded
* disconnected
* backend-dependent

Do NOT start massive implementation before understanding the architecture.

## Phase 2 — Core Domain Fixes

Fix foundational problems first:

1. Unified merchant model
2. Unified commission model
3. Shared domain/mock data
4. State consistency
5. Repository/service boundaries
6. Centralized permissions

## Phase 3 — Critical Missing Features

Implement:

1. Merchant registration
2. Merchant onboarding
3. KYC/documents
4. Contract acceptance
5. Bank/payout details
6. Merchant approval
7. Catalogue approval

## Phase 4 — Commercial Features

Implement:

1. Merchant advertising
2. Merchant subscriptions
3. Revenue management
4. Payout/statement improvements
5. Merchant performance/health

## Phase 5 — Ecosystem Features

Implement where appropriate:

1. Staff
2. Multi-property
3. Disputes
4. Notifications
5. Channel manager/PMS connection UI

## Phase 6 — Verification

Run build/typecheck/tests and verify the complete flows.

---

# 30. Token Efficiency Rules

This is important.

Do NOT waste tokens by:

* repeatedly reading the same files
* explaining obvious code
* rewriting entire files unnecessarily
* dumping large code blocks into the conversation
* exploring unrelated dependencies
* changing working components without reason
* generating documentation before implementation
* repeatedly summarizing the same findings

Instead:

1. Inspect targeted files.
2. Build a concise mental model.
3. Identify dependencies.
4. Make focused changes.
5. Verify immediately.
6. Move to the next phase.

When a phase is complete, provide only a concise summary of:

* what changed
* important files changed
* verification result
* remaining issues

---

# 31. Do Not Stop at UI

A feature is NOT considered implemented simply because a page exists.

For every feature verify:

```text
Route
→ UI
→ State
→ Domain Model
→ Validation
→ Business Logic
→ Mock Data
→ Related Modules
```

If any layer is missing, complete it.

---

# 32. Do Not Overbuild

This is still a prototype.

Do not add:

* unnecessary backend infrastructure
* databases
* Docker
* API servers
* authentication servers
* payment gateways
* cloud storage
* external integrations

unless the existing project already contains them and they are required for the current frontend prototype.

Build only what is necessary to make the prototype complete and architecturally ready.

---

# 33. Final Acceptance Criteria

The task is complete only when:

* No major missing merchant lifecycle stage remains in the frontend prototype.
* Merchant data has one authoritative domain source.
* Commission calculations use one consistent unit.
* Catalogue approval and publishing are connected.
* Merchant onboarding is functional as a prototype.
* KYC/document workflow exists as a prototype.
* Contract acceptance exists.
* Payout details exist.
* Merchant financial views are consistent.
* Advertising is available to merchants.
* Merchant subscription plans exist.
* Merchant staff/permissions are represented.
* Multi-property structure is supported where applicable.
* Dispute participation exists where applicable.
* Notifications have a coherent event-driven mock structure.
* Existing booking/inventory/pricing/settlement functionality still works.
* No major UI-vs-domain contradictions remain.
* No unnecessary backend/database has been introduced.
* The architecture is ready for future API integration.
* Typecheck/build/tests pass.

---

# Final Rule

**Do not assume the analysis document is perfectly correct.**

Verify every claim against the current repository.

**Do not assume the existing code is correct either.**

If you discover a better implementation while auditing, fix it when it is directly relevant to correctness, consistency, maintainability, or backend readiness.

Prioritize:

**Correctness → Domain consistency → Complete user flows → Backend readiness → UX polish**

Do not sacrifice existing working functionality for unnecessary refactoring.

Start with **Phase 1: Audit**, then proceed systematically through the phases and implement the required changes.
