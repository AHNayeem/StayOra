/**
 * Marketing module — the growth surfaces built on data the platform already
 * has: abandoned checkouts, computed customer segments, campaigns and the
 * waitlist. None of them own a dataset; each reads the domain.
 */
export { RecoveryView } from "./recovery-view";
export { CampaignsView } from "./campaigns-view";
export { SegmentsView } from "./segments-view";
export { WaitlistView } from "./waitlist-view";
